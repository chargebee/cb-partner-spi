---
openapi: post /api/v2/credit_notes/{document_id}/einvoice_status
---

## Form-URL-Encoded Array Parameters

This endpoint accepts `application/x-www-form-urlencoded` data. For array fields, use bracket notation as shown below.

### Related Documents

The `related_documents` field is an array of related document status objects. Each object contains:

<ParamField body="related_documents" field="document_id" />
<ParamField body="related_documents" field="document_type" />
<ParamField body="related_documents" field="status" />
<ParamField body="related_documents" field="timestamp" />
<ParamField body="related_documents" field="provider_response" />
<ParamField body="related_documents" field="errors" />

**Example:**
```
related_documents[0][document_id]=appresp_7gH1KLM
related_documents[0][document_type]=ubl-application-response
related_documents[0][status]=SUCCESS
related_documents[0][timestamp]=2025-09-04T08:17:23.380Z
```

### Errors

The `errors` field is an array of error objects. Each object contains:

<ParamField body="errors" field="code" />
<ParamField body="errors" field="message" />
<ParamField body="errors" field="help_url" />

**Example:**
```
errors[0][code]=MISSING_REQUIRED_DATA
errors[0][message]=The request did not contain a required property
errors[0][help_url]=https://docs.example.com/errors
```